<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:backgroundTint="@color/colorRojoNetflix"
    android:foregroundTint="@color/colorRojoNetflix">

    <ProgressBar
        android:id="@+id/layoutCarga"
        android:layout_width="667dp"
        android:layout_height="524dp"
        android:layout_gravity="center"
        android:foregroundTint="@color/colorRojoNetflix"
        android:indeterminateTint="@color/colorRojoNetflix"
        android:progressTint="@color/colorRojoNetflix" />

</FrameLayout>